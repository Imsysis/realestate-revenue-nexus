// Update this page (the content is just a fallback if you fail to update the page)

const Index = () => {
  // This component redirects to Home component
  // The routing is now handled in App.tsx
  return null;
};

export default Index;
